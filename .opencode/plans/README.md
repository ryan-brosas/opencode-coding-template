# Local Plans

This directory is reserved for generated plan artifacts from OpenCode sessions.

Generated `*.md` files are ignored by git so the template stays clean. Move reusable workflow guidance to `.opencode/protocols/` instead.
